

import YAML from 'yaml'
export function objToYaml(obj) {
    try {
        const doc = new YAML.Document()
        doc.contents = obj
        return doc.toString()
    } catch (e) {
        return obj
    }
}